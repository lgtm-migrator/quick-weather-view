import { IQuickLink } from '@/utils/quickLink';

interface IQuickLinkItems {
  quickLinkItems: IQuickLink[];
}

export function quickLinkItmes() {
  const editQuickLinkItems = ({ id, domain, name, thumbnail }: IQuickLink) => {};
}
